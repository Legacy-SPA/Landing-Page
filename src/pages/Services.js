import React from "react"
import { makeStyles } from "@material-ui/core/styles"

const useStyles = makeStyles((theme) => {
  console.log(theme)
  return ({

    }
  )}
)

const Services = () => (
  <div>
    <h1>This is a MainMenu</h1>
  </div>
)

export default Services
